import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { useState } from 'react';

export default function TicketDetails({ _id, authorized }: { _id: string, authorized: boolean }) {
  return (<div>
    Ticket Details
  </div>);
}
